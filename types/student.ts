export interface Student {
    id: string;
    name: string;
    school: string;
    grade: string;
    // status: "Pending" | "Approved" | "Rejected";
    submissionDate: string;
    // familyInfo: string;
    // academicInfo: string;
}
